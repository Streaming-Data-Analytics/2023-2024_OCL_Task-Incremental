name: SDAenv2
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
    - numpy<2  
    - torch==2.1.2
    - torchvision==0.8.3  
    - avalanche-lib==0.4.0
    - matplotlib
    - ipykernel 
